rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All reads and writes go through server-side authorization.
    match /{document=**} { allow read, write: if false; }
  }
}
